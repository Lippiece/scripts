{
  "$schema": "https://json.schemastore.org/package",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "name": "scripts",
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "build": "bun build --production --outdir dist --entrypoints src/* && sed -i 's#//!#//#' dist/*.js"
  },
  "type": "module",
  "version": "1.1.0"
}
