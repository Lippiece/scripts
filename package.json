{
  "$schema": "https://json.schemastore.org/package",
  "name": "scripts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --production --outdir dist --entrypoints src/*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
