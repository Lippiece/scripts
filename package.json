{
  "name": "scripts",
  "version": "1.10.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rolldown src/* -d dist -m && sed -i 's#//!#//#' dist/*.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "rolldown": "^1.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
